<?xml version="1.0" encoding="utf-8"?>
<!--
  DEBUG ONLY. This file exists in src/debug and never ships in a release APK.

  It permits cleartext to the local development CRM only:
    - 10.0.2.2  is the host machine as seen from the Android emulator
    - localhost is the device itself, for adb reverse port forwarding

  Everything else still refuses cleartext, so a debug build pointed at a real host is as strict as
  release. User-added CA certificates are deliberately NOT trusted even here, so that a proxy cannot
  silently read traffic during development.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
