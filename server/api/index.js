/**
 * Vercel entry point.
 *
 * The Express app is exported rather than started; the platform owns the listener. Everything else
 * about the service is identical to running `npm start` locally.
 */
module.exports = require('../src/index.js');
